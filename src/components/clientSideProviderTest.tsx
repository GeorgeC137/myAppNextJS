"use client"
import { ClientSideProviderTestProps } from '@/lib/types'


const ClientSideProviderTest = ({ children }: ClientSideProviderTestProps) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default ClientSideProviderTest
